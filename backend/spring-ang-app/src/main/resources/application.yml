server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/booksappdb
    username: root
    password:
    driver-class-name: com.mysql.jdbc.Driver
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: 'true'

